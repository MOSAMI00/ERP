# سجل التعديلات (Edits Log)

هذا الملف يحتوي على سجل بكافة التعديلات التي تم إجراؤها على المشروع، مع شرح الأسباب والاقتراحات.

---

## [2026-05-14] إنشاء Admin Seeder وإصلاح التصميم

### 1. `database/seeders/AdminSeeder.php`
*   **ماذا عدلت**: إنشاء Seeder جديد يقوم بإنشاء رتبة "super_admin" ومستخدم أدمن افتراضي.
*   **لماذا**: لتسهيل عملية إعداد بيئة التطوير وتسجيل الدخول للوحة التحكم لأول مرة.
*   **اقتراحات**: إضافة المزيد من الأدوار الافتراضية (مثل Support, Finance) مع صلاحيات محدودة.

### 2. `database/seeders/DatabaseSeeder.php`
*   **ماذا عدلت**: استدعاء `AdminSeeder` ضمن مصفوفة الـ seeders.
*   **لماذا**: ليتم تنفيذ Seeder الأدمن تلقائياً عند تشغيل `php artisan db:seed`.
*   **اقتراحات**: لا يوجد.

### 3. `resources/css/admin.css`
*   **ماذا عدلت**: إضافة `@import "tailwindcss";` في بداية الملف وتعديل المتغيرات.
*   **لماذا**: لأن Tailwind v4 يتطلب الاستيراد الصريح في ملفات الـ CSS لتوليد الفئات المخصصة، وبدونه لم تكن ألوان لوحة التحكم تظهر.
*   **اقتراحات**: دمج ملفات الـ CSS الخاصة بالأدمن والمستخدم في شجرة CSS واحدة لتقليل حجم الطلبات.

## [2026-05-14] تحديث نظام التحقق من الهوية (KYC Workflow)


### 1. `database/migrations/2026_05_14_191310_add_selfie_url_to_kyc_documents_table.php`
*   **ماذا عدلت**: إضافة حقل `selfie_url` لجدول الوثائق.
*   **لماذا**: لتمكين النظام من تخزين الصورة الشخصية (Selfie) كجزء من عملية التحقق.
*   **اقتراحات**: إضافة حقل `metadata` لتخزين معلومات إضافية عن الصورة (مثل الحجم أو النوع) مستقبلاً.

### 2. `app/Models/KycDocument.php`
*   **ماذا عدلت**: إضافة `selfie_url` إلى قائمة الحقول القابلة للتعبئة (`$fillable`).
*   **لماذا**: لتمكين الحفظ الجماعي (Mass Assignment) للحقل الجديد في قاعدة البيانات.
*   **اقتراحات**: إضافة `Accessor` لتحويل الرابط إلى URL كامل تلقائياً عند جلب البيانات.

### 3. `app/Http/Requests/StoreKycDocumentRequest.php`
*   **ماذا عدلت**: إضافة قاعدة تحقق `selfie_image` (مطلوب، صورة، لا تتعدى 4 ميجابايت).
*   **لماذا**: لضمان أن المستخدم يرفع صورة شخصية صحيحة وذات جودة مناسبة.
*   **اقتراحات**: إضافة قواعد تحقق أكثر صرامة (مثل أبعاد الصورة) لضمان وضوح الوجه.

### 4. `app/Http/Controllers/KycDocumentController.php`
*   **ماذا عدلت**: 
    1. معالجة رفع ملف `selfie_image` وتخزينه في المسار الصحيح.
    2. تعديل اسم الخاصية المرسلة للواجهة من `documents` إلى `kyc_documents`.
*   **لماذا**: 
    1. لإتمام عملية الحفظ الفعلي للملف.
    2. ليتوافق مع ما يتوقعه مكون `Settings.jsx` في الواجهة الأمامية.
*   **اقتراحات**: استخدام `Service Class` لمعالجة رفع الملفات بدلاً من كتابتها مباشرة في الـ Controller لزيادة قابلية إعادة الاستخدام.

### 5. `resources/js/Pages/settings/ui/KYCUploaders.jsx`
*   **ماذا عدلت**: إضافة واجهة لرفع الصورة الشخصية، وتحديث منطق التحقق من إمكانية الإرسال (`canSubmit`).
*   **لماذا**: ليتمكن المستخدم من رؤية الخطوات المطلوبة ورفع كافة الملفات في طلب واحد.
*   **اقتراحات**: إضافة ميزة "معاينة الصورة" (Preview) قبل الإرسال لتحسين تجربة المستخدم.

### 6. `resources/js/Pages/Admin/Kyc/Index.jsx`
*   **ماذا عدلت**: 
    1. إعادة بناء الصفحة بالكامل لتطابق تصميم "المستخدمين" البريميوم.
    2. إضافة شريط فلاتر (Filter Bar) مع ميزة التبديل السريع بين الحالات (قيد المراجعة، مقبول، مرفوض).
    3. عرض بيانات المستخدم (الاسم، البريد، الصورة الشخصية) بشكل احترافي.
    4. تحسين عرض صور الوثائق مع تأثيرات حركية (Hover) وأيقونات فتح في نافذة جديدة.
    5. استخدام مكونات النظام الموحدة (`Badge`, `Button`, `Table`).
*   **لماذا**: لضمان اتساق التصميم عبر كافة صفحات لوحة الإدارة ولحل مشكلة المظهر البسيط الذي نتج عن دمج المشاريع.
*   **اقتراحات**: إضافة فلتر للبحث عن المستخدمين بالاسم وتفعيل زر تصدير البيانات (CSV).

---
